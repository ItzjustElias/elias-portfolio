const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
    extend: {
      colors: {
        background: "#0a0a0a",
        primary: "#ffee00",
      },
    },
  },
};

export default config;
